name: Deploy to EC

on:
  push:
    branches:
      # - main
      - develop

jobs:
  deploy_main:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    env:
      SSH_PRIVATE_KEY: SSH_PRIVATE_KEY
    steps:
      - name: Deploy to EC2 (Main)
        uses: ./actions/deploy-to-ec2.yml

  # deploy_develop:
  #   if: github.ref == 'refs/heads/develop'
  #   runs-on: ubuntu-latest
  #   env:
  #     SSH_PRIVATE_KEY: SSH_PRIVATE_KEY
  #   steps:
  #     - name: Deploy to EC2 (Develop)
  #       uses: ./actions/deploy-to-ec2.yml
